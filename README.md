## moment-update-time-format

### Usage

In the index of your file, at top most, before you use moment, make sure to import this package. Because this modifies the original `format` function to work with this.
